<form method="post" action="{{ path('app_reservation_delete', {'numres': reservation.numres}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ reservation.numres) }}">
    <button class="btn">Delete</button>
</form>
