<form method="post" action="{{ path('app_reservation2_delete', {'numres': reservation2.numres}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ reservation2.numres) }}">
    <button class="btn">Delete</button>
</form>
